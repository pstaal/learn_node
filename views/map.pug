extends layout 

block content 
  .innner
    h2= title
      .map 
        .autocomplete
          input.autocomplete__input(type="text" placeholder="Search for anything!" name="geolocate")
        #map 
          p Loading Map...